[pytest]
# Sagt pytest-qt, welche Qt-Bindung verwendet wird (nur PySide6 ist in requirements.txt
# gelistet - ohne diese Angabe würde pytest-qt sonst versuchen, automatisch zu erkennen,
# welche Bindung installiert ist).
qt_api = pyside6
