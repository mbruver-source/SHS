# Zusätzliche Abhängigkeiten NUR für die Tests (CI: tests.yml, lokal bei Bedarf) - fließen
# weder in den Desktop-Installer noch in das Web-/Container-Image ein.
#
# pytest/pytest-qt: Testlauf inkl. der PySide6-GUI-Tests (test_app_gui.py).
# pypdf: liest die erzeugten PDFs in test_pdf_export.py wieder ein. Ohne pypdf wird die
# komplette Klasse TestPdfExport STILL übersprungen - bis 22.09. war das auch in der CI der
# Fall (Codeprüfung 22.09., M4), d. h. die PDF-Inhaltstests liefen nirgends.
#
# Obergrenzen (Codeprüfung 22.09., G12): jeweils "<" die nächste Major-Version NACH der
# aktuell (Stand 22.09.2026) neuesten (pytest 9.1.x, pytest-qt 4.5.x, pypdf 6.19.x), damit
# ein künftiger Major-Sprung die CI nicht unbemerkt ändert - Untergrenzen unverändert.
pytest>=8.0,<10
pytest-qt>=4.4,<5
pypdf>=4.0,<7
